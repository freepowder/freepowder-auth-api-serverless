get:
  tags:
    - authentication
  summary: De authenticate users
  description: Returns the user and the jwt auth token as null
  operationId: signOut
  responses:
    "200":
      description: success
      content:
        application/json:
          schema:
            $ref: "../schemas/auth.yml"
