post:
  tags:
    - home
  summary: home
  description: home
  operationId: home
  responses:
    "200":
      description: success
      content:
        application/json:
          schema:
            $ref: "../schemas/home.yml"
    default:
      $ref: "../responses/error.yml"
