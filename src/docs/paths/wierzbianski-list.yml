get:
  tags:
    - wierzbianski
  summary: Get the content for wierzbianski site
  description: Returns content for the site
  operationId: getWierzbianskiContentList
  responses:
    "200":
      description: success
      content:
        application/json:
          schema:
            $ref: "../schemas/wierzbianski-list.yml"
    default:
      $ref: "../responses/error.yml"
