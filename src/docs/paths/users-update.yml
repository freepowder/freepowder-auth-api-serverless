put:
  tags:
    - users
  summary: Update a user
  description: Returns content for the site
  operationId: setUser
  parameters:
    - in: path
      name: userId
      schema:
        type: string
      required: true
  responses:
    "200":
      description: success
      content:
        application/json:
          schema:
            $ref: "../schemas/user.yml"
